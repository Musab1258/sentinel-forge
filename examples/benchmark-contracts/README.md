# Benchmark Contract Fixtures

These fixtures are meant for performance-oriented analyzer work and future benchmark automation.

## Included examples

- `small_registry.rs`: light parsing baseline
- `nested_branching.rs`: conditional complexity
- `storage_heavy.rs`: repeated state updates and operation density

Keep these fixtures readable. Their purpose is to expose scale characteristics, not to mimic production code exactly.
