# SDK

This package now provides the local phase 7 and phase 8 integration surface for:

- advisory and vulnerability-intelligence records
- plugin manifest and registry shapes
- future API or dashboard consumers that should not depend on raw file formats directly

The package is intentionally lightweight for now. It defines stable TypeScript shapes before a network API exists.
