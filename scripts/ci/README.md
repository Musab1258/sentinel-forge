# CI Scripts

These scripts back the local and GitHub Actions workflows added for later-phase readiness.

## Scripts

- `run-checks.sh`: landing-page tests, Rust workspace tests, secure CI gate, and vulnerable scan export
- `export-demo-reports.sh`: reproducible JSON and HTML analyzer exports for demos and artifacts
